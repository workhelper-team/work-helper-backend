spring:
  application:
    name: work-helper-backend

  datasource:
    url: jdbc:postgresql://workhelper-db.cl8s8em6ox0v.ap-northeast-2.rds.amazonaws.com:5432/workhelper
    username: postgres
    password: helper1234!

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 12MB

server:
  error:
    include-message: always

storage:
  type: ${STORAGE_TYPE:local}
  local-root: ${STORAGE_LOCAL_ROOT:uploads}
  s3:
    bucket: ${STORAGE_S3_BUCKET:}